import { StyleSheet } from 'react-native';


export const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginBottom:16,
    paddingHorizontal: 32
  },
   value: {
    fontSize:14,
    fontWeight:'700',
    color: '#fff',
    textAlign:'justify',
  }
});